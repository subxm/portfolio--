/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.jsdelivr.net',
      'www.svgrepo.com',
      'images.unsplash.com'
    ],
  },
}

export default nextConfig